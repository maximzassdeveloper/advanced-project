export enum ArticleBlockType {
  CODE = 'CODE',
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
}

export enum ArticleView {
  LIST = 'LIST',
  GRID = 'GRID',
}

export enum ArticleSortBy {
  CREATED_AT = 'created_at',
  POPULAR = 'popular',
}
